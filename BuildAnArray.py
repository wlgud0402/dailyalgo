class Solution:
    def buildArray(self, target, n):
        answer = []
        for i in range(len(target)):
            try:
                if target[i] == i+1
        

solution=Solution()

print(solution.buildArray([1,2,3],3))
#출력 : ["Push","Push","Push"]